# LogPilot MCP Example

This example demonstrates how to interact with the LogPilot MCP Server. In practice, the server is meant to be run as an executable.

To run the server:
```sh
dart run log_pilot_mcp --vm-service-uri=ws://127.0.0.1:8181/ws
```
